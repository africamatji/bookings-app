version: '3'
services:
    front-end:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '3000:3000'
        command: sh -c "yarn install && yarn build && yarn start"
        networks:
            - sail
networks:
    sail:
        driver: bridge
volumes:
    sail-mysql:
        driver: local
